namespace GScore.Domain.Constants;

public enum ScoreLevel
{
    Excellent,      // >= 8.0
    Good,           // >= 6.0 && < 8.0
    Average,        // >= 4.0 && < 6.0
    BelowAverage    // < 4.0
}
